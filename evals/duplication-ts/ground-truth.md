# Ground truth — duplication-ts

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `ts-duplication`

`src/receipt.js` re-implements `currency()` from the unchanged `src/format.js`. Finding it requires reading a file the change did not touch.
