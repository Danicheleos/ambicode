# Ground truth — degraded-ts

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `ts-rename-delete`

One file is renamed and another deleted while tests still import the old names. Affected tests cannot be enumerated from vanished paths. This case measures what the arm says when it cannot know.
