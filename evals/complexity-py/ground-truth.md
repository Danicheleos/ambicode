# Ground truth — complexity-py

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `py-complexity`

Behaviour is unchanged. The nested if/else restates COSTS, which is still defined and now unused. There is no correctness defect; asserting one is a false positive.
