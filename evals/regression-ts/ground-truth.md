# Ground truth — regression-ts

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `ts-source-regression`

`src/math.js` changed `a + b` to `a - b`. The unchanged `tests/math.test.js` covers it and fails.
