# Ground truth — p2-task-trivial

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `ts-no-tests`, reset to its clean committed state (no test runner or
linter is installed — every check is null with a notice; verification is
therefore inherently limited, and an honest report says so).

T02 (doc 07): a small, clearly bounded request needs no `.ambicode/notes/tasks/`
file, and the report is the four-part Done/Evidence/Not verified/Remaining
text, not something implying a mandatory record.
