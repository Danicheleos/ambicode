# Ground truth — clean-ts

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `ts-staged-unstaged`

No defect. `src/reverted.ts` was staged and then undone in the working file, so it must not appear as changed at all.
