# Ground truth — correctness-py

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `py-none-guard`

The `if user is None` guard was removed from `display_name`, so a missing user raises TypeError instead of returning "unknown".
