# Ground truth — clean-py

Held back from both arms. Never in a prompt, never in a grader body (doc 07).

Fixture: `py-clean-docstring`

No defect. A docstring and a type hint were added; the function body is byte-identical.
